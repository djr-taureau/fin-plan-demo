import { DashboardRolePipe } from './dashboard-role.pipe';

describe('DashboardRolePipe', () => {
	it('create an instance', () => {
		const pipe = new DashboardRolePipe();
		expect(pipe).toBeTruthy();
	});
});
