{
  "name": "lifeworks.aadb2c.ui",
  "version": "1.0.0",
  "description": "Authentication UI for Lifeworks Azure Active Directory B2C Tenant",
  "author": "ezra@teambrightly.com",
  "scripts": {
    "build": "dotnet build",
    "build:release": "rm -rf ./dist/; dotnet publish -f netcoreapp2.1 -c Release --output ./dist/",
    "zip": "cd ./dist; zip lifeworks.aadb2c.ui.zip -rm .",
    "package": "npm run build:release; npm run zip;",
    "deploy": "az webapp deployment source config-zip --src ./dist/lifeworks.aadb2c.ui.zip",
    "package:deploy": "npm run package; npm run deploy"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.1"
  }
}
